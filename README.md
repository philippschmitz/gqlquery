# gqlquery
Bash script to query GraphQL API with gql files via curl.

## Usage

```
Usage: gqlquery [-p] [-t TOKEN] FILE URL
Query a GraphQL API with a .gql file.

  -p Pretty print output
  -t TOKEN  Bearer token for authentication
```

